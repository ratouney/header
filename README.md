# Header Generator for C projects

Clone it recursively, else you won't have the needed library to compile it :
    git clone --recursive http://github.com/ratouney/header.git
    
Just type --help if you really have no fucking idea of what you are doing...
